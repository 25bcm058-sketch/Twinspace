<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base platform theme; the real theme is Compose Material 3 (ui/theme/Theme.kt). -->
    <style name="Theme.TwinSpace" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
